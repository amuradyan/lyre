# 2025-Oct-12

## Issues from previous note

Code storage per slide currently uses local storage. As Lyre grows, this approach may need rethinking since we'll want to work on parts of the language while running the whole system.

## On different views of a slide

Some elements of lyre /most of it, actually/, can be written both with iteration and recursion. I'd like to provide the freedom of choice for a reader to pick either, or even both, and see how they compare. I'd still like to have a single slide on topic, so the paths or views should be encoded in the note markdown. That can be rendered as tabs or a dropdown or a neat toggle.

----

Since Lyre is streaming at heart, when we get to sampling the wave, I won't take the greedy path first, but calculating samples and putting them in an array, hence no need to discuss the recursive approach there. Instead, I will  introduce the JS generators and `yield` keyword to produce samples on demand and show how they can be iterated over with `for...of` loop.
