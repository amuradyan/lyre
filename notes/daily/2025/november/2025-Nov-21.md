# Daily Note - November 21, 2025

## Issues from Previous Note

The hint toggle functionality on the string operations exercises slide is broken - when exercises are solved and hints are toggled, they appear and disappear immediately. The initial hint stripping logic isn't working properly on page load for these specific exercises.

## On Tokenizer Implementation Strategy

The current implementation of the tokenizer will use `let`, since we've introduced it in earlier slides. However, it would be valuable to also present a recursive approach as a parallel track. This would give students exposure to both imperative and functional approaches to the same problem, enriching their understanding of different programming paradigms.

Not sure whether to introduce `switch` for the tokenizer or stick with `if-else`. Will probably start with `if-else` and see where it goes - can always refactor to `switch` later if the case handling becomes too verbose.

## On Slide Navigator UX

The current slide navigator with collapsable sections is not comfortable. Need easier section switching - should probably try a tabbed interface and think through the options.

----

Done.
