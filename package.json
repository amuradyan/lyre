{
  "name": "lyre-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "lang",
    "course"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=course",
    "build": "npm run build --workspace=course",
    "test": "npm run test --workspace=lang && npm run test --workspace=course"
  }
}
