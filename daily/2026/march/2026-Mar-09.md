# Daily Note - March 9, 2026

## On set! special form

Implemented `set!` for creating and updating bindings. Returns empty generator `(function*() {})()` to maintain uniform interface - all expressions return generators, runner just iterates.

Not happy with this approach - would be better to skip returning a generator entirely, but this is how it works for now.
