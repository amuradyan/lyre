# Daily Note - March 12, 2026

So we have `let` as the only way to create bindings. we have `-(` and `=(` as syntax sugar for `sequence` and `envelope` and `|` for bars as in sheet music to visually split the parts.
