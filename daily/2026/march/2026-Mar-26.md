# Daily Note - March 26, 2026

## Issues from Previous Notes

- `harmony` should be rewritten to parallel, and `sequence` should get a friend that applies a fadeout between notes. `mix` needs a friend too - need to think more about primitive counterparts and sound smoothing.

## On visual tools

### The environment

At some point it was decided to introduce sane defaults into the language, so we can start writing music right away, without settings the ADSR and the step. Once we introduce waves, most probably the wave will also be exported into env and get a default. Generally, I decided to embrace the environment with defaults and its' overall role in the system, so I think it should be easy to inspect any time.

I am thinking we extend the lyre code block and attack a table to the right of it that shows the env. It can be walkable as well, but we'll leave that for later.

### On what's being evaluated

Another thing, I was thinking about is to highlight the expression we are evaluating. Again this has to do with the lyre codeblock but it will also require some changes to the evaluator.

Maybe we can return the expression or an id of it somehow along with the musical data?
