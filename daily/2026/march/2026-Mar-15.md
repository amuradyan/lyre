# Daily Note - March 15, 2026

## Issues from Previous Notes

*Mar 14*

- Rewrite Lyre to use a directly implemented `(play 3 C4)` instead of dot notation desugaring - then lead with the inconvenience of bespoke functions toward thinking about moving it into the language

----

And how do we calculate gate time? We do (play X C4) but how do we compute X? To do that we need `.` value and we will get it only in runtime. One way to do it is to introduce the `times` operator and do `(play (times 3 .) C4)`

Or we can assume play handles number times dot inside and don't worry at all. After all play is provided by the engine.

----

This has been done, `play` handles `.`.
