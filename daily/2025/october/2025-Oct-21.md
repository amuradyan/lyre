# 2025-Oct-21

## Issues from previous note

- Testing infinite generators remains unresolved - current approach exhausts generators into arrays which won't work for infinite sequences

## On code blocks that generate audio

Such blocks do not have tests but might require code edits. I am thinking of disabling the play button unless the code compiles without errors. Once the code compiles, the play button can be enabled to allow audio playback. Every time the code compiles successfully, it should be automatically saved to local storage, overriding the previous version, to preserve edits across page reloads.
