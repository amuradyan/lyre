# Daily Note - December 4, 2025

## Issues from Previous Note

The hint toggle functionality on the string operations exercises slide is broken - when exercises are solved and hints are toggled, they appear and disappear immediately. The initial hint stripping logic isn't working properly on page load for these specific exercises.

The current slide navigator with collapsible sections is not comfortable. Need easier section switching - should probably try a tabbed interface and think through the options.

----

Done.

## On check of any kind

Till slide 44, we are assuming only the happy path. It would be nice to have a set of safety checks. Maybe the next iteration could be it.

## On equality

We do `==` for equality, but in JS we'd rather do  `===`. I need to find the first place where we used the `==`, and introduce and rewrite to `===`.

## On recursion

In [this](../../2025/october/2025-Oct-12.md) note, I speak of recursion and how it can be included in the course. Turns out, the first place we are able to introduce the recursion smoothly is the `interpret`. To make it an iteration, we need to introduce a stack, just like for tokenization. I'd like to not do that though, given how nice and subtle is the introduction.

## On sync of slide code and the engine

We have a separate project till now for the engine and Lyre codeblocks run on that. We also got to a point when we can `play` a lyre code as a string in JS. These two are not in sync. We need to do that for one and then figure out a way to store the slide code in files and referencing that. ideally that would be the engine.

! I need a flow of doing the same iteration on both slides and the engine.
